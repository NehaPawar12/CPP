#include <iostream>
using namespace std;

int main()
{
    //Arithematic operator : +,-,*,/,%(gives reminder)
    int num1 = 23;
    int num2 = 12;

    cout<<num1+num2<<endl;//float/int, float/float, int/float = float.

    //Logical operator: &&(both should be true then the answer is true.), ||(atleast 1 should be true), !

    //Relational operator: <, >,<=, >=, ==(Check), !=.

    //Unary operator. ++, --: a++ is post, ++a is pre.
    return 0;
}